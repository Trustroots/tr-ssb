{
  "scripts": {
    "whoami": "ssb-server whoami -- --path ./data",
    "friends": "ssb-server friends.get -- --path ./data",
    "start": "ssb-server start -- --path ./data"
  },
  "dependencies": {
    "ssb-server": "^15.3.0"
  }
}
